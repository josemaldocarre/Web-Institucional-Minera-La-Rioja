import { SectionPageLayout } from '../../layouts/SectionPageLayout'
import { CONTACTO_PAGE } from '../../navigation/sectionNav'

export default function Contacto() {
  return (
    <SectionPageLayout config={CONTACTO_PAGE}>
      <div>Contacto</div>
    </SectionPageLayout>
  )
}
